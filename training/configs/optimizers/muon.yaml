# Muon optimizer — momentum-based, no adaptive LR
optimizer:
  name: "muon"
  lr: 2.0e-2  # Muon typically uses higher LR
  muon_momentum: 0.95
  muon_nesterov: true

logging:
  wandb_run_name: "ministral-muon"
